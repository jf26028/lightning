@using Lightning
@{
	Layout = "_Layout.cshtml";

	dynamic requestData = Context.GetRequestData();
	Page.Title = requestData.Content.Title;
}
<article>
	@Html.Raw(requestData.Content.GetContent())
</article>
